{
  try {
    long count=0;
    long read;
    while ((read=in.read(ByteStreams.skipBuffer)) != -1) {
      count+=read;
    }
    return count;
  }
  finally {
    Arrays.fill(ByteStreams.skipBuffer,(byte)0);
  }
}
