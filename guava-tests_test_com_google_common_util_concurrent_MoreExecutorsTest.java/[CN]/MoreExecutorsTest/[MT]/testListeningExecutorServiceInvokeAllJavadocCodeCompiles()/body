{
  ListeningExecutorService executor=newDirectExecutorService();
  List<Callable<T>> tasks=ImmutableList.of();
  @SuppressWarnings("unchecked") List<ListenableFuture<T>> unused=(List)executor.invokeAll(tasks);
}
