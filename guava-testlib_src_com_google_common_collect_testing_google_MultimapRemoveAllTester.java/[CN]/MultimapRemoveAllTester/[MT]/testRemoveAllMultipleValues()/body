{
  resetContainer(Helpers.mapEntry(k0(),v0()),Helpers.mapEntry(k0(),v1()),Helpers.mapEntry(k0(),v2()));
  assertThat(multimap().removeAll(k0())).containsExactly(v0(),v1(),v2());
  assertTrue(multimap().isEmpty());
}
