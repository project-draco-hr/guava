{
  CacheBuilder<Object,Object> builder=new CacheBuilder<Object,Object>().refreshInterval(3600,SECONDS);
  try {
    builder.refreshInterval(3600,SECONDS);
    fail();
  }
 catch (  IllegalStateException expected) {
  }
}
