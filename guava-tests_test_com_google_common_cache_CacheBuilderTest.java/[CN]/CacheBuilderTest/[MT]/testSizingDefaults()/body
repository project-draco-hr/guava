{
  Cache<?,?> cache=CacheBuilder.newBuilder().build(identityLoader());
  LocalCacheInternalMap<?,?> map=CacheTesting.toLocalCacheInternalMap(cache);
  assertEquals(4,map.segments.length);
  assertEquals(4,map.segments[0].table.length());
}
