{
  if (TypeToken.of(getParameterTypes()[paramIndex]).getRawType().isPrimitive()) {
    return true;
  }
  Annotation[] annotations=getParameterAnnotations()[paramIndex];
  for (  Annotation annotation : annotations) {
    if (annotation instanceof Nullable) {
      return true;
    }
  }
  return false;
}
