{
  for (BiEntry<K,V> entry=hashTableVToK[smearedKeyHash & mask]; entry != null; entry=entry.nextInValueBucket) {
    if (entry.matchesValue(value,smearedKeyHash)) {
      return entry;
    }
  }
  return null;
}
