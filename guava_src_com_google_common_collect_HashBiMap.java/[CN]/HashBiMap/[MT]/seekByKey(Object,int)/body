{
  for (BiEntry<K,V> entry=hashTableKToV[smearedKeyHash & mask]; entry != null; entry=entry.nextInKeyBucket) {
    if (entry.matchesKey(key,smearedKeyHash)) {
      return entry;
    }
  }
  return null;
}
