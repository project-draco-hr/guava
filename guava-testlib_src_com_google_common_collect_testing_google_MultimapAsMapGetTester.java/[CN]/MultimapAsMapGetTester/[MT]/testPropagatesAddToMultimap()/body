{
  Collection<V> result=multimap().asMap().get(sampleKeys().e0());
  result.add(sampleValues().e3());
  assertThat(multimap().get(sampleKeys().e0())).containsExactly(sampleValues().e0(),sampleValues().e3());
}
