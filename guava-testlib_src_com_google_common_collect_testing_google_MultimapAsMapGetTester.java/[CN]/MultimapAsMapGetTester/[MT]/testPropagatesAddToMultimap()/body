{
  Collection<V> result=multimap().asMap().get(sampleKeys().e0);
  result.add(sampleValues().e3);
  ASSERT.that(multimap().get(sampleKeys().e0)).has().exactly(sampleValues().e0,sampleValues().e3);
}
