{
  SortedMap<String,String> map=new SafeTreeMap<String,String>(Ordering.natural().nullsFirst());
  for (  Entry<String,String> entry : entries) {
    map.put(entry.getKey(),entry.getValue());
  }
  return map.entrySet();
}
