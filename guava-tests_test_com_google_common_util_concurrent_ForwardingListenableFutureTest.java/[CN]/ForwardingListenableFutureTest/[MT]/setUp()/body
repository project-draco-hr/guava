{
  super.setUp();
  delegate=SettableFuture.create();
  forwardingFuture=new ForwardingListenableFuture<String>(){
    @Override protected ListenableFuture<String> delegate(){
      return delegate;
    }
  }
;
  tester=new ListenableFutureTester(forwardingFuture);
  tester.setUp();
}
