{
  assertThat(findClassesToTest(ImmutableList.of(Foo.class))).has().exactly(Foo.class).inOrder();
  assertThat(findClassesToTest(ImmutableList.of(Foo.class,Foo2Test.class))).has().exactly(Foo.class).inOrder();
}
