{
  sanityTests.ignoreClasses(Predicates.<Object>equalTo(PublicFoo.class));
  ASSERT.that(findClassesToTest(ImmutableList.of(PublicFoo.class))).isEmpty();
  ASSERT.that(findClassesToTest(ImmutableList.of(Foo.class))).hasContentsInOrder(Foo.class);
}
