{
  Random random=new Random();
  for (int i=0; i < 100; i++) {
    byte[] bytes=new byte[64];
    random.nextBytes(bytes);
    String s=new String(bytes,"UTF-16LE");
    assertEquals(new Sink(4).putString(s).hash(),new Sink(4).putBytes(s.getBytes("UTF-16LE")).hash());
  }
}
