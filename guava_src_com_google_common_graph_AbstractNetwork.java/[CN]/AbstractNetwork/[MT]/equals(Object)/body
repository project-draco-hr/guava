{
  if (obj == this) {
    return true;
  }
  if (!(obj instanceof Network)) {
    return false;
  }
  Network<?,?> other=(Network<?,?>)obj;
  if (isDirected() != other.isDirected() || !nodes().equals(other.nodes()) || !edges().equals(other.edges())) {
    return false;
  }
  for (  E edge : edges()) {
    if (!incidentNodes(edge).equals(other.incidentNodes(edge))) {
      return false;
    }
  }
  return true;
}
