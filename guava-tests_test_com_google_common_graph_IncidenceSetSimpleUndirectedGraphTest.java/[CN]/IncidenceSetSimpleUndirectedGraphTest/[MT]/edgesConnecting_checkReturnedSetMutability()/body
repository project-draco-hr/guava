{
  addNode(N1);
  addNode(N2);
  Set<String> edgesConnecting=undirectedGraph.edgesConnecting(N1,N2);
  try {
    edgesConnecting.add(E23);
    fail(ERROR_MODIFIABLE_SET);
  }
 catch (  UnsupportedOperationException e) {
    addEdge(E12,N1,N2);
    assertThat(undirectedGraph.edgesConnecting(N1,N2)).containsExactlyElementsIn(edgesConnecting);
  }
}
