{
  checkState(valueStrength == null,"Value strength was already set to " + valueStrength + ".");
  valueStrength=checkNotNull(strength);
  if (strength != Strength.STRONG) {
    useCustomMap=true;
  }
  return this;
}
