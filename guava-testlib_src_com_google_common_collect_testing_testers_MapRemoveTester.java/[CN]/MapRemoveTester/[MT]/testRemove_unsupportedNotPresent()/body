{
  try {
    assertNull("remove(notPresent) should return null or throw " + "UnsupportedOperationException",getMap().remove(e3().getKey()));
  }
 catch (  UnsupportedOperationException tolerated) {
  }
  expectUnchanged();
  expectMissing(e3());
}
