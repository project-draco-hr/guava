{
  if (map.refreshes() && (now - entry.getWriteTime() > map.refreshNanos)) {
    V newValue=refresh(key,hash,loader);
    if (newValue != null) {
      return newValue;
    }
  }
  return oldValue;
}
