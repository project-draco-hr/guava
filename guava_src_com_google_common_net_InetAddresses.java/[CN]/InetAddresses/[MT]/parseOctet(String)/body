{
  int octet=Integer.parseInt(ipPart);
  if (ipPart.startsWith("-") || octet > 255 || (ipPart.startsWith("0") && ipPart.length() > 1)) {
    throw new NumberFormatException();
  }
  return (byte)octet;
}
