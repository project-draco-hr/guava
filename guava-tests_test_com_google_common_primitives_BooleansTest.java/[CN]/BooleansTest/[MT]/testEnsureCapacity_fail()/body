{
  try {
    boolean[] unused=Booleans.ensureCapacity(ARRAY_FALSE,-1,1);
    fail();
  }
 catch (  IllegalArgumentException expected) {
  }
  try {
    boolean[] unused=Booleans.ensureCapacity(ARRAY_FALSE,1,-1);
    fail();
  }
 catch (  IllegalArgumentException expected) {
  }
}
