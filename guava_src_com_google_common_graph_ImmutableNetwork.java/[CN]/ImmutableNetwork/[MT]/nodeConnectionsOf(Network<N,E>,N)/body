{
  return graph.isDirected() ? DirectedNodeConnections.ofImmutable(graph.predecessors(node),graph.successors(node),graph.inEdges(node),graph.outEdges(node)) : UndirectedNodeConnections.ofImmutable(graph.adjacentNodes(node),graph.incidentEdges(node));
}
