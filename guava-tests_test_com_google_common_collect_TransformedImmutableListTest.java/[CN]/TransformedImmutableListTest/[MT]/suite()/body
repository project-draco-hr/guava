{
  return ListTestSuiteBuilder.using(new TestStringListGenerator(){
    @SuppressWarnings("serial") @Override protected List<String> create(    String[] elements){
      return new TransformedImmutableList<String,String>(ImmutableList.copyOf(elements)){
        @Override String transform(        String str){
          return str;
        }
      }
;
    }
  }
).named("TransformedImmutableList identity").withFeatures(CollectionSize.ANY,CollectionFeature.ALLOWS_NULL_QUERIES).createTestSuite();
}
