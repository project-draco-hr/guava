{
  return new Scheduler(){
    @Override public Future<?> schedule(    ScheduledExecutorService service,    Runnable task){
      return service.scheduleAtFixedRate(task,initialDelay,period,unit);
    }
  }
;
}
