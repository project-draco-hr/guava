{
  if (root == null) {
    return 0;
  }
 else   if (range.tooHigh(root.getKey())) {
    return 1 + countOrZero(root.childOrNull(RIGHT)) + countTooHigh(range,root.childOrNull(LEFT));
  }
 else {
    return countTooHigh(range,root.childOrNull(RIGHT));
  }
}
