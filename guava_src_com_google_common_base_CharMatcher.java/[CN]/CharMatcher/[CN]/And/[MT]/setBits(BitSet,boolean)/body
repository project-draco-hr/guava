{
  BitSet tmp1=new BitSet();
  first.setBits(tmp1,true);
  BitSet tmp2=new BitSet();
  second.setBits(tmp2,true);
  tmp1.and(tmp2);
  if (value) {
    table.or(tmp1);
  }
 else {
    table.andNot(tmp1);
  }
}
