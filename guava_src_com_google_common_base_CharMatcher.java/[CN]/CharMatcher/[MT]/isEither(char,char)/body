{
  String toString=new StringBuilder("CharMatcher.anyOf(\"").append(match1).append(match2).append("\")").toString();
  return new FastMatcher(toString){
    @Override public boolean matches(    char c){
      return c == match1 || c == match2;
    }
    @GwtIncompatible("java.util.BitSet") @Override void setBits(    BitSet table){
      table.set(match1);
      table.set(match2);
    }
  }
;
}
