{
  return new BSTPathFactory<K,N,BSTInOrderPath<K,N>>(){
    @Override public BSTInOrderPath<K,N> extension(    BSTInOrderPath<K,N> path,    BSTSide side){
      return BSTInOrderPath.extension(path,side);
    }
    @Override public BSTInOrderPath<K,N> initialPath(    N root){
      return new BSTInOrderPath<K,N>(root,null,null);
    }
  }
;
}
