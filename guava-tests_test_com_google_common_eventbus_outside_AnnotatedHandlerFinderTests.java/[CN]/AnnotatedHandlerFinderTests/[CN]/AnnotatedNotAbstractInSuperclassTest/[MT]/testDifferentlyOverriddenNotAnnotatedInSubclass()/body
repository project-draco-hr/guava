{
  ASSERT.that(getHandler().differentlyOverriddenNotAnnotatedInSubclassGoodEvents).hasContentsInOrder(EVENT);
  ASSERT.that(getHandler().differentlyOverriddenNotAnnotatedInSubclassBadEvents).isEmpty();
}
