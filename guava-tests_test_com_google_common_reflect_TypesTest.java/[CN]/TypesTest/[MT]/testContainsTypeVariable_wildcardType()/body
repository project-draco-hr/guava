{
  assertFalse(Types.containsTypeVariable(new TypeCapture<Iterable<? extends String>>(){
  }
.capture()));
  assertFalse(Types.containsTypeVariable(new TypeCapture<Iterable<? super String>>(){
  }
.capture()));
}
