{
  assertEquals(expected.toString(),actual.toString());
  assertEquals(actual.toString(),expected.hashCode(),actual.hashCode());
  ASSERT.that(actual.getLowerBounds()).has().exactlyAs(asList(expected.getLowerBounds())).inOrder();
  ASSERT.that(actual.getUpperBounds()).has().exactlyAs(asList(expected.getUpperBounds())).inOrder();
}
