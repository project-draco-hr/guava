{
  checkNotNull(graph);
  return new NetworkBuilder<Object,Object>(graph.isDirected()).allowsParallelEdges(graph.allowsParallelEdges()).allowsSelfLoops(graph.allowsSelfLoops()).nodeOrder(graph.nodeOrder()).edgeOrder(graph.edgeOrder()).cast();
}
