{
  if (object instanceof GraphConfig) {
    GraphConfig that=(GraphConfig)object;
    return this.multigraph == that.multigraph && this.selfLoopsAllowed == that.selfLoopsAllowed && this.expectedNodeCount.equals(that.expectedNodeCount) && this.expectedEdgeCount.equals(that.expectedEdgeCount);
  }
  return false;
}
