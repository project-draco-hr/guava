{
  if (key == null) {
    return null;
  }
  int i;
  try {
    i=SortedLists.binarySearch(Lists.transform(entries,keyFunction),key,unsafeComparator(),EQUAL,false);
  }
 catch (  ClassCastException e) {
    return null;
  }
  return i >= 0 ? entries.get(i).getValue() : null;
}
