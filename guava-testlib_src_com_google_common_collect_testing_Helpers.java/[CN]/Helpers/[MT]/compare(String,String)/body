{
  if (lhs == rhs) {
    return 0;
  }
  if (lhs == null) {
    if (rhs.equals(B)) {
      return -1;
    }
    return B.compareTo(rhs);
  }
  if (rhs == null) {
    if (lhs.equals(B)) {
      return 1;
    }
    return lhs.compareTo(B);
  }
  return lhs.compareTo(rhs);
}
