{
  try {
    double[] unused=Doubles.ensureCapacity(ARRAY1,-1,1);
    fail();
  }
 catch (  IllegalArgumentException expected) {
  }
  try {
    double[] unused=Doubles.ensureCapacity(ARRAY1,1,-1);
    fail();
  }
 catch (  IllegalArgumentException expected) {
  }
}
