{
  K k0=e0().getKey();
  K k1=e1().getKey();
  V v0=e0().getValue();
  getMap().put(k0,v0);
  try {
    getMap().put(k1,v0);
    fail("Expected IllegalArgumentException");
  }
 catch (  IllegalArgumentException expected) {
  }
  expectAdded(e0());
}
