{
  K k0=samples.e0.getKey();
  K k1=samples.e1.getKey();
  V v0=samples.e0.getValue();
  getMap().put(k0,v0);
  try {
    getMap().put(k1,v0);
    fail("Expected IllegalArgumentException");
  }
 catch (  IllegalArgumentException expected) {
  }
  Helpers.assertContentsAnyOrder(getMap().entrySet(),Helpers.mapEntry(k0,v0));
  Helpers.assertContentsAnyOrder(getMap().inverse().entrySet(),Helpers.mapEntry(v0,k0));
}
