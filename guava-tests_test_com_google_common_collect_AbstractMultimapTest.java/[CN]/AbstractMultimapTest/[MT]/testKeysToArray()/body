{
  multimap.put("foo",1);
  multimap.put("foo",5);
  multimap.put("foo",nullValue());
  multimap.put(nullKey(),3);
  ASSERT.that(multimap.keys().toArray()).has().allOf("foo","foo","foo",nullKey());
  ASSERT.that(multimap.keys().toArray(new String[3])).has().allOf("foo","foo","foo",nullKey());
}
