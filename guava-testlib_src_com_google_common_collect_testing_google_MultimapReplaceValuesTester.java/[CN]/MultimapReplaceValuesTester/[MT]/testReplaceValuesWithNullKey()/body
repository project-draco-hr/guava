{
  int size=multimap().size();
  K key=null;
  @SuppressWarnings("unchecked") List<V> values=Arrays.asList(sampleValues().e0(),sampleValues().e2(),sampleValues().e3());
  multimap().replaceValues(key,values);
  assertGet(key,values);
}
