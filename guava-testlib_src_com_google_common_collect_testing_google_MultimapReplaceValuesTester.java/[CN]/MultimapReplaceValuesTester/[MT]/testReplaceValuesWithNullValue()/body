{
  int size=multimap().size();
  K key=sampleKeys().e0();
  @SuppressWarnings("unchecked") List<V> values=Arrays.asList(sampleValues().e0(),null,sampleValues().e3());
  multimap().replaceValues(key,values);
  assertGet(key,values);
}
