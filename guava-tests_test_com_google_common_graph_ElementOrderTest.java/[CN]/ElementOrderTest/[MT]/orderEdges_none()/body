{
  MutableNetwork<Integer,String> graph=NetworkBuilder.directed().orderEdges(unordered()).build();
  assertThat(graph.edgeOrder()).isEqualTo(unordered());
  assertThat(graph.nodeOrder()).isEqualTo(insertion());
}
