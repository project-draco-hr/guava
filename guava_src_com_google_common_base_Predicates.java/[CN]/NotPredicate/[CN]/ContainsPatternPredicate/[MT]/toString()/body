{
  String patternString=Objects.toStringHelper(pattern).add("pattern",pattern).add("pattern.flags",Integer.toHexString(pattern.flags())).toString();
  return "Predicates.contains(" + patternString + ")";
}
