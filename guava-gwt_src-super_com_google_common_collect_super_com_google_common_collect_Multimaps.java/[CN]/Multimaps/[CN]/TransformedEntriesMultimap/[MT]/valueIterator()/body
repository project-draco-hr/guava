{
  return Iterators.transform(fromMultimap.entries().iterator(),new Function<Entry<K,V1>,V2>(){
    @Override public V2 apply(    final Entry<K,V1> entry){
      return transformer.transformEntry(entry.getKey(),entry.getValue());
    }
  }
);
}
