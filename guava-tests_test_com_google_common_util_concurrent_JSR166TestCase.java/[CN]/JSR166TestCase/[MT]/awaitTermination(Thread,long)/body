{
  try {
    t.join(timeoutMillis);
  }
 catch (  InterruptedException ie) {
    threadUnexpectedException(ie);
  }
 finally {
    if (t.isAlive()) {
      t.interrupt();
      fail("Test timed out");
    }
  }
}
