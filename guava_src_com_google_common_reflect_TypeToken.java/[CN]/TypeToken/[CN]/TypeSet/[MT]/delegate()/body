{
  ImmutableSet<TypeToken<? super T>> filteredTypes=types;
  if (filteredTypes == null) {
    return (types=ImmutableSet.copyOf(Iterables.filter(findAllTypes(),TypeFilter.IGNORE_TYPE_VARIABLE_OR_WILDCARD)));
  }
 else {
    return filteredTypes;
  }
}
