{
  ImmutableSet<TypeToken<? super T>> filteredTypes=types;
  if (filteredTypes == null) {
    return (types=ImmutableSet.copyOf(Sets.filter(findAllTypes(),TypeFilter.IGNORE_TYPE_VARIABLE_OR_WILDCARD)));
  }
 else {
    return filteredTypes;
  }
}
