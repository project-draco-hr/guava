{
  if (runtimeType.equals(formalType)) {
    return true;
  }
  if (formalType instanceof WildcardType) {
    return every(((WildcardType)formalType).getUpperBounds()).isSupertypeOf(runtimeType) && every(((WildcardType)formalType).getLowerBounds()).extendsFrom(runtimeType);
  }
  return false;
}
