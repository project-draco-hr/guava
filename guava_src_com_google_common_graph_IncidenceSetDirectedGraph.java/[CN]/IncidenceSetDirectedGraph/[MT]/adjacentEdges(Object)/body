{
  checkNotNull(edge,"edge");
  DirectedIncidentNodes<N> endpoints=edgeToIncidentNodes.get(edge);
  checkArgument(endpoints != null,EDGE_NOT_IN_GRAPH,edge);
  return Sets.difference(Sets.union(incidentEdges(endpoints.target()),incidentEdges(endpoints.source())),ImmutableSet.of(edge));
}
