{
synchronized (watch) {
    watch.stop();
    logger.log(Level.FINE,"Started {0} in {1} ms.",new Object[]{service,startupTimeMillis()});
  }
  state.serviceFinishedStarting(service,currentlyHealthy);
}
