{
  int slice=0;
  for (int i=0; i < bitsPerSlice; i++) {
    int nextBit=(bytes[byteIndex] >>> bitIndex) & 1;
    slice=(slice << 1) | nextBit;
    bitIndex++;
    if (bitIndex == 8) {
      bitIndex=0;
      byteIndex++;
    }
  }
  return slice;
}
