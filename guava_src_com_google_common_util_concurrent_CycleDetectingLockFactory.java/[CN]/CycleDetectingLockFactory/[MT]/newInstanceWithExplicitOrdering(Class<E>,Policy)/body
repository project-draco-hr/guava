{
  checkNotNull(enumClass);
  checkNotNull(policy);
  @SuppressWarnings("unchecked") Map<E,LockGraphNode> lockGraphNodes=(Map<E,LockGraphNode>)lockGraphNodesPerType.get(enumClass);
  return new WithExplicitOrdering<E>(policy,lockGraphNodes);
}
