{
  GeneralRange<Integer> range=GeneralRange.range(Ordering.natural(),2,OPEN,4,CLOSED);
  assertTrue(range.intersect(GeneralRange.range(Ordering.natural(),5,CLOSED,6,CLOSED)).isEmpty());
  assertTrue(range.intersect(GeneralRange.range(Ordering.natural(),1,OPEN,2,OPEN)).isEmpty());
}
