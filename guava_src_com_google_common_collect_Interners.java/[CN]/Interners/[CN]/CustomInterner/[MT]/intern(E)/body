{
  while (true) {
    ReferenceEntry<E,Dummy> entry=map.getLiveEntry(sample);
    if (entry != null) {
      return entry.getKey();
    }
    Dummy sneaky=map.putIfAbsent(sample,Dummy.VALUE);
    if (sneaky == null) {
      return sample;
    }
 else {
    }
  }
}
