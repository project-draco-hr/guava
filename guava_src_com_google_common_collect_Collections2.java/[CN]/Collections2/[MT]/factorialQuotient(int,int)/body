{
  checkArgument(n > 0);
  checkArgument(d > 0);
  checkArgument(n >= d);
  long result=1;
  for (int i=d + 1; i <= n; i++) {
    result*=i;
    if (result < 0) {
      throw new IllegalArgumentException("Numeric overflow");
    }
  }
  return result;
}
