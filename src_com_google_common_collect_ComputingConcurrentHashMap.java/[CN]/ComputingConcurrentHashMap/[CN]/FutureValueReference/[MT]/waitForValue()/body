{
  boolean success=false;
  try {
    V value=ComputingConcurrentHashMap.this.waitForValue(original);
    success=true;
    return value;
  }
  finally {
    if (!success) {
      removeEntry();
    }
  }
}
