{
  resetContainer(Helpers.mapEntry(sampleKeys().e0,sampleValues().e0),Helpers.mapEntry(sampleKeys().e0,sampleValues().e1),Helpers.mapEntry(sampleKeys().e1,sampleValues().e0));
  Multiset<K> keys=multimap().keys();
  assertEquals(2,keys.count(sampleKeys().e0));
  assertEquals(1,keys.count(sampleKeys().e1));
  assertEquals(3,keys.size());
  assertThat(keys).containsAllOf(sampleKeys().e0,sampleKeys().e1);
  assertThat(keys.entrySet()).containsAllOf(Multisets.immutableEntry(sampleKeys().e0,2),Multisets.immutableEntry(sampleKeys().e1,1));
}
