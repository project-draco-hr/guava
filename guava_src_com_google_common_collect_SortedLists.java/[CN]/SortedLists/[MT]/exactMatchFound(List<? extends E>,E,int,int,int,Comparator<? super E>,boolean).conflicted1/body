{
  if (!worryAboutDuplicates) {
    return index;
  }
  lower=index;
  while (lower < upper) {
    int middle=(lower + upper + 1) >>> 1;
    int c=comparator.compare(list.get(middle),e);
    if (c > 0) {
      upper=middle - 1;
    }
 else {
      lower=middle;
    }
  }
  return lower;
}
