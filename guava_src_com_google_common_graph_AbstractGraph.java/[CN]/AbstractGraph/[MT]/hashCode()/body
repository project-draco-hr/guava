{
  Function<N,Set<N>> nodeToSuccessors=new Function<N,Set<N>>(){
    @Override public Set<N> apply(    N node){
      return successors(node);
    }
  }
;
  return Maps.asMap(nodes(),nodeToSuccessors).hashCode();
}
