{
  return new TransformedIterator<R,Entry<R,Map<C,V>>>(backingMap.keySet().iterator()){
    @Override Entry<R,Map<C,V>> transform(    R rowKey){
      return Maps.immutableEntry(rowKey,row(rowKey));
    }
  }
;
}
