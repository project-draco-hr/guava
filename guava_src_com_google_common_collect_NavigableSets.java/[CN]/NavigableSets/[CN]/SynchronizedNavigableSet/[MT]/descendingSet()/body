{
synchronized (mutex) {
    if (descendingSet == null) {
      NavigableSet<E> dS=synchronizedNavigableSet(delegate().descendingSet(),mutex);
      descendingSet=dS;
      return dS;
    }
    return descendingSet;
  }
}
