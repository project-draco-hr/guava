{
  assertFreshInstance(new TypeToken<ImmutableSortedMap<String,Integer>>(){
  }
);
  FreshValueGenerator generator=new FreshValueGenerator();
  assertValueAndTypeEquals(ImmutableSortedMap.of(generator.generate(String.class),generator.generate(int.class)),new FreshValueGenerator().generate(new TypeToken<ImmutableSortedMap<String,Integer>>(){
  }
));
  assertValueAndTypeEquals(ImmutableSortedMap.of(),new FreshValueGenerator().generate(new TypeToken<ImmutableSortedMap<EmptyEnum,String>>(){
  }
));
}
