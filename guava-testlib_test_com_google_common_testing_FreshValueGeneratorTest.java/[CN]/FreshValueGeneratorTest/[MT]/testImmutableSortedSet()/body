{
  assertFreshInstance(new TypeToken<ImmutableSortedSet<String>>(){
  }
);
  assertValueAndTypeEquals(ImmutableSortedSet.of(new FreshValueGenerator().generate(String.class)),new FreshValueGenerator().generate(new TypeToken<ImmutableSortedSet<String>>(){
  }
));
  assertValueAndTypeEquals(ImmutableSortedSet.of(),new FreshValueGenerator().generate(new TypeToken<ImmutableSortedSet<EmptyEnum>>(){
  }
));
}
