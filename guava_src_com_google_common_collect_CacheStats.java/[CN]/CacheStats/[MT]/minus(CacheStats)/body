{
  return new CacheStats(Math.max(0,hitCount - other.hitCount),Math.max(0,missCount - other.missCount),Math.max(0,createSuccessCount - other.createSuccessCount),Math.max(0,createExceptionCount - other.createExceptionCount),Math.max(0,totalCreateTime - other.totalCreateTime),Math.max(0,evictionCount - other.evictionCount));
}
