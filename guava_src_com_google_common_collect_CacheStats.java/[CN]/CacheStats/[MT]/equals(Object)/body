{
  if (object instanceof CacheStats) {
    CacheStats other=(CacheStats)object;
    return hitCount == other.hitCount && missCount == other.missCount && createCount == other.createCount && totalCreateTime == other.totalCreateTime && evictionCount == other.evictionCount;
  }
  return false;
}
