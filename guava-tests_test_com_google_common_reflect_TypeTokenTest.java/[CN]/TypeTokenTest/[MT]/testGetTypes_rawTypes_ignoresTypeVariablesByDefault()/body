{
  TypeToken<?>.TypeSet types=TypeToken.of(new TypeCapture<B>(){
  }
.capture()).getTypes();
  makeUnmodifiable(types.rawTypes()).has().exactly(Interface1.class,Class1.class,Object.class);
  makeUnmodifiable(types.interfaces().rawTypes()).has().exactly(Interface1.class).inOrder();
  makeUnmodifiable(types.classes().rawTypes()).has().exactly(Class1.class,Object.class).inOrder();
}
