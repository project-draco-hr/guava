{
  TypeToken<?>.TypeSet types=TypeToken.of(new TypeCapture<B>(){
  }
.capture()).getTypes();
  ASSERT.that(types.rawTypes()).hasContentsAnyOrder(Interface1.class,Class1.class,Object.class);
  ASSERT.that(types.interfaces().rawTypes()).hasContentsInOrder(Interface1.class);
  ASSERT.that(types.classes().rawTypes()).hasContentsInOrder(Class1.class,Object.class);
}
