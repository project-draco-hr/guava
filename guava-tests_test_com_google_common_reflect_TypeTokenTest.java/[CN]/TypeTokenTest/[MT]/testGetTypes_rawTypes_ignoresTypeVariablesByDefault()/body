{
  TypeToken<?>.TypeSet types=TypeToken.of(new TypeCapture<B>(){
  }
.capture()).getTypes();
  assertThat(types.rawTypes()).has().exactly(Interface1.class,Class1.class,Object.class);
  assertThat(types.interfaces().rawTypes()).has().exactly(Interface1.class).inOrder();
  assertThat(types.classes().rawTypes()).has().exactly(Class1.class,Object.class).inOrder();
}
