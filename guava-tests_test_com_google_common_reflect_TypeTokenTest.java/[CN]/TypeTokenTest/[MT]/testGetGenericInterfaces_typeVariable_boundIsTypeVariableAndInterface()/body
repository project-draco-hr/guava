{
  ASSERT.that((List)TypeToken.of(new TypeCapture<T2>(){
  }
.capture()).getGenericInterfaces()).iteratesAs(TypeToken.of(new TypeCapture<T1>(){
  }
.capture()));
  assertHasArrayInterfaces(new TypeToken<T2[]>(){
  }
);
}
