{
  assertThat(TypeToken.of(new TypeCapture<T2>(){
  }
.capture()).getGenericInterfaces()).has().allOf(TypeToken.of(new TypeCapture<T1>(){
  }
.capture()));
  assertHasArrayInterfaces(new TypeToken<T2[]>(){
  }
);
}
