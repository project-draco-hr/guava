{
  TypeToken<?>.TypeSet types=TypeToken.of(new TypeCapture<A>(){
  }
.capture()).getTypes();
  assertThat(types.rawTypes()).has().allOf(Interface1.class,Interface2.class,Interface3.class,Iterable.class);
}
