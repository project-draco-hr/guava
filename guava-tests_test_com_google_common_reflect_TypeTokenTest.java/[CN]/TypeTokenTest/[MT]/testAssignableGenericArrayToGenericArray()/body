{
  assertTrue(new TypeToken<T[]>(){
  }
.isAssignableFrom(new TypeToken<T[]>(){
  }
));
  assertTrue(new TypeToken<T[]>(){
  }
.isAssignableFrom(new TypeToken<T1[]>(){
  }
));
  assertFalse(new TypeToken<T[]>(){
  }
.isAssignableFrom(new TypeToken<T[][]>(){
  }
));
}
