{
  assertTrue(TypeToken.of(Object[].class).isAssignableFrom(new TypeToken<T[]>(){
  }
));
  assertTrue(TypeToken.of(Object[].class).isAssignableFrom(new TypeToken<T[][]>(){
  }
));
  assertTrue(TypeToken.of(Object[][].class).isAssignableFrom(new TypeToken<T[][]>(){
  }
));
}
