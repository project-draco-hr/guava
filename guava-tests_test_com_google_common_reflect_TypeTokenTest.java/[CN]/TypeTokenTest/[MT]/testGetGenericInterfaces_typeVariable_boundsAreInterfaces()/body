{
  ASSERT.that(TypeToken.of(new TypeCapture<T>(){
  }
.capture()).getGenericInterfaces()).iteratesOverSequence(TypeToken.of(CharSequence.class),new TypeToken<Iterable<String>>(){
  }
);
  assertHasArrayInterfaces(new TypeToken<T[]>(){
  }
);
}
