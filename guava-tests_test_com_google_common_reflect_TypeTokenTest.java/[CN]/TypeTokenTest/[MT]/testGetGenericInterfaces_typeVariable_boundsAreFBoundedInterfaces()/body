{
  ASSERT.that((List)TypeToken.of(new TypeCapture<T>(){
  }
.capture()).getGenericInterfaces()).iteratesAs(TypeToken.of(CharSequence.class),new TypeToken<Iterable<T>>(){
  }
);
  assertHasArrayInterfaces(new TypeToken<T[]>(){
  }
);
}
