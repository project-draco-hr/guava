{
  assertThat(TypeToken.of(new TypeCapture<T>(){
  }
.capture()).getGenericInterfaces()).has().allOf(TypeToken.of(CharSequence.class),new TypeToken<Iterable<T>>(){
  }
);
  assertHasArrayInterfaces(new TypeToken<T[]>(){
  }
);
}
