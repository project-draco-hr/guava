{
  long duration=42;
  TimeUnit unit=SECONDS;
  CustomConcurrentHashMap<Object,Object> map=makeMap(createMapMaker().expireAfterWrite(duration,unit));
  assertEquals(unit.toNanos(duration),map.expireAfterWriteNanos);
}
