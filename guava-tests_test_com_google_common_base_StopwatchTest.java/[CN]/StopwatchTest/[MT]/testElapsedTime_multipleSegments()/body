{
  stopwatch.start();
  ticker.advance(9);
  stopwatch.stop();
  ticker.advance(16);
  stopwatch.start();
  assertEquals(9,stopwatch.elapsedTime(NANOSECONDS));
  ticker.advance(25);
  assertEquals(34,stopwatch.elapsedTime(NANOSECONDS));
  stopwatch.stop();
  ticker.advance(36);
  assertEquals(34,stopwatch.elapsedTime(NANOSECONDS));
}
