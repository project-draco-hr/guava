{
  checkNotNull(node,"node");
  if (node.equals(nodeA)) {
    return nodeB;
  }
 else   if (node.equals(nodeB)) {
    return nodeA;
  }
 else {
    throw new IllegalArgumentException(String.format("Endpoints %s is not incident to node %s",this,node));
  }
}
