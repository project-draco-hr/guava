{
  try {
    long total=0;
    while (true) {
      int read=in.read(skipBuffer);
      if (read == -1) {
        break;
      }
      total+=read;
    }
    return total;
  }
  finally {
    Arrays.fill(skipBuffer,(byte)0);
  }
}
