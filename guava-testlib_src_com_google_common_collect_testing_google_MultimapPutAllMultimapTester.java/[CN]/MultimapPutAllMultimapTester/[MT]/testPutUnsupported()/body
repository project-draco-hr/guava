{
  try {
    multimap().putAll(getSubjectGenerator().create(Helpers.mapEntry(sampleKeys().e3(),sampleValues().e3())));
    fail("Expected UnsupportedOperationException");
  }
 catch (  UnsupportedOperationException expected) {
  }
}
