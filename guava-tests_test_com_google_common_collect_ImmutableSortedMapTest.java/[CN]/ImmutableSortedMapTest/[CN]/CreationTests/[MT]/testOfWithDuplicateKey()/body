{
  try {
    ImmutableSortedMap.of("one",1,"one",1);
    fail();
  }
 catch (  IllegalArgumentException expected) {
    assertEquals("Duplicate keys in mappings one=1 and one=1",expected.getMessage());
  }
}
