{
  Map<String,Integer> map=ImmutableSortedMap.of("one",1,"two",2,"three",3).headMap("three",true);
  ASSERT.that(map.entrySet()).has().allOf(Maps.immutableEntry("one",1),Maps.immutableEntry("three",3)).inOrder();
}
