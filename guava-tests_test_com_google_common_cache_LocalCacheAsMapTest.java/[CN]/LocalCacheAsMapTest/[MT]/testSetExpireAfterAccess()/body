{
  long duration=42;
  TimeUnit unit=TimeUnit.SECONDS;
  LocalCacheAsMap<Object,Object> map=makeMap(createCacheBuilder().expireAfterAccess(duration,unit));
  assertEquals(unit.toNanos(duration),map.expireAfterAccessNanos);
}
