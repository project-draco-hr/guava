{
  Preconditions.checkPositionIndex(start,size);
  return new AbstractIndexedIterator<E>(start,size){
    @SuppressWarnings("unchecked") @Override protected E get(    int index){
      return (E)array[index + offset];
    }
  }
;
}
