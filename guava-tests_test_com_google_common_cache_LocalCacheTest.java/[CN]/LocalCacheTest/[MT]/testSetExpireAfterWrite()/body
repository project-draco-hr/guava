{
  long duration=42;
  TimeUnit unit=TimeUnit.SECONDS;
  LocalCache<Object,Object> map=makeMap(createCacheBuilder().expireAfterWrite(duration,unit));
  assertEquals(unit.toNanos(duration),map.expireAfterWriteNanos);
}
