{
  File jarFile=new File("base/some.jar");
  Manifest manifest=manifestClasspath("current.jar");
  assertThat(ClassPath.Scanner.getClassPathFromManifest(jarFile,manifest)).has().exactly(new File("base/current.jar").toURI()).inOrder();
}
