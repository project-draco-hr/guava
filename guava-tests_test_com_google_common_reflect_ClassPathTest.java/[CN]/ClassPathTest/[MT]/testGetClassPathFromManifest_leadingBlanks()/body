{
  File jarFile=new File("base/some.jar");
  Manifest manifest=manifestClasspath(" relative.jar");
  ASSERT.that(ClassPath.getClassPathFromManifest(jarFile,manifest)).hasContentsInOrder(new File("base/relative.jar").toURI());
}
