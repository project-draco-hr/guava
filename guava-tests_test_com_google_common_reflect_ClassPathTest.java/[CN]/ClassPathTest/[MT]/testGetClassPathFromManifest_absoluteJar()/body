{
  File jarFile=new File("base/some.jar");
  Manifest manifest=manifestClasspath("file:/with/absolute.jar");
  ASSERT.that(ClassPath.getClassPathFromManifest(jarFile,manifest)).hasContentsInOrder(new File("/with/absolute.jar").toURI());
}
