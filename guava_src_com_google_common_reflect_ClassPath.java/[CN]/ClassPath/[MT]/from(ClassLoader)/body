{
  final ImmutableSortedSet.Builder<ResourceInfo> resources=new ImmutableSortedSet.Builder<ResourceInfo>(Ordering.usingToString());
  for (  Entry entry : getAllEntries(classloader)) {
    resources.add(entry.toResourceInfo());
  }
  return new ClassPath(resources.build());
}
