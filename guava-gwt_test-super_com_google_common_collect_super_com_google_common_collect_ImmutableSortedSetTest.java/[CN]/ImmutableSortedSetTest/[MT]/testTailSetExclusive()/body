{
  String[] strings=NUMBER_NAMES.toArray(new String[0]);
  ImmutableSortedSet<String> set=ImmutableSortedSet.copyOf(strings);
  Arrays.sort(strings);
  for (int i=0; i < strings.length; i++) {
    ASSERT.that(set.tailSet(strings[i],false)).has().allFrom(sortedNumberNames(i + 1,strings.length)).inOrder();
  }
}
