{
  SortedSet<String> set=ImmutableSortedSet.copyOf(STRING_LENGTH,asIterator("in","the","quick","jumped","over","a"));
  assertThat(set).has().exactly("a","in","the","over","quick","jumped").inOrder();
}
