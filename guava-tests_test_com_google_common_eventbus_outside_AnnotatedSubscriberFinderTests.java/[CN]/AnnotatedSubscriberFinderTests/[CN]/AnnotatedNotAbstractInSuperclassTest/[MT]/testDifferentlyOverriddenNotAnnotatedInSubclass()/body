{
  ASSERT.that(getSubscriber().differentlyOverriddenNotAnnotatedInSubclassGoodEvents).has().item(EVENT);
  ASSERT.that(getSubscriber().differentlyOverriddenNotAnnotatedInSubclassBadEvents).isEmpty();
}
