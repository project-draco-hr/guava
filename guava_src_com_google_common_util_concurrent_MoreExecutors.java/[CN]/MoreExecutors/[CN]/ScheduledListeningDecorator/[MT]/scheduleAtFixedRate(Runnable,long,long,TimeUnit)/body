{
  ListenableFutureTask<Void> task=ListenableFutureTask.create(command,null);
  Delayed delayed=delegate.scheduleAtFixedRate(task,initialDelay,period,unit);
  return new ListenableScheduledTask<Void>(task,delayed);
}
