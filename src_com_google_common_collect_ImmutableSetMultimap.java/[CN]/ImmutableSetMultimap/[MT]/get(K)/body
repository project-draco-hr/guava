{
  ImmutableSet<V> set=(ImmutableSet<V>)map.get(key);
  if (set != null) {
    return set;
  }
 else   if (emptySet != null) {
    return emptySet;
  }
 else {
    return ImmutableSet.<V>of();
  }
}
